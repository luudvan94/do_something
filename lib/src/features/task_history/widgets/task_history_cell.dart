import 'package:do_something/src/features/models/task_history.dart';
import 'package:flutter/material.dart';

class TaskHistoryCell extends StatelessWidget {
  final TaskHistory taskHistory;

  const TaskHistoryCell({Key? key, required this.taskHistory})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
